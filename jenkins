pipeline {
    agent any
    triggers {
        githubPush()
    }
    stages {
        stage('Build') {
            when {
                branch 'dev'
            }
            steps {
                echo "Running build on develop branch"
            }
        }
    }
}
